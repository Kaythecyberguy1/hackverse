# Server
PORT=8080
PUBLIC_HOST=http://localhost             # change to https://labs.yourdomain.com in prod

# Docker connection (use one)
DOCKER_SOCKET=/var/run/docker.sock
# DOCKER_HOST=tcp://127.0.0.1:2375       # alternative: remote docker host

# Instances
DEFAULT_TTL_MINUTES=60                   # auto-stop/remove after N minutes
ALLOWED_HOST_PORT_RANGE=30000-39999      # host port range for lab instances

# Optional simple auth (shared secret)
API_KEY=changeme
